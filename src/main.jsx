import React from 'react';
import ReactDOM from 'react-dom/client';
import MakeUi from './component';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
	<React.StrictMode>
		<MakeUi />
	</React.StrictMode>
);
